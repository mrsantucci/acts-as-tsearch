sqlite:
  :adapter: sqlite
  :dbfile: plugin.sqlite.db
sqlite3:
  :adapter: sqlite3
  :dbfile: ":memory:"
postgresql:
  :adapter: postgresql
  :username: acts_as_tsearch_test
  :password: acts_as_tsearch_test
  :database: acts_as_tsearch_test
  :min_messages: ERROR
  test:
    adapter: postgresql
    database: activeagent_test
    username: postgres
    password: redrum
    host: localhost
mysql:
  :adapter: mysql
  :host: localhost
  :username: rails
  :password:
  :database: plugin_test
  
  
  
  # MySQL (default setup).  Versions 4.1 and 5.0 are recommended.
  #
  # Install the MySQL driver:
  #   gem install mysql
  # On MacOS X:
  #   gem install mysql -- --include=/usr/local/lib
  # On Windows:
  #   There is no gem for Windows.  Install mysql.so from RubyForApache.
  #   http://rubyforge.org/projects/rubyforapache
  #
  # And be sure to use new-style password hashing:
  #   http://dev.mysql.com/doc/refman/5.0/en/old-client.html
  #development:
  #  adapter: mysql
  #  database: activeAgent_dev
  #  username: root
  #  password: redrum
  #  host: localhost

  development:
    adapter: postgresql
    database: activeagent_development
    username: activeagent
    password: radiant$pace
    host: localhost

  devprod:
    adapter: postgresql
    database: activeagent_devv
    username: postgres
    password: redrum
    host: localhost


  # Warning: The database defined as 'test' will be erased and
  # re-generated from your development database when you run 'rake'.
  # Do not set this db to the same as development or production.
  test:
    adapter: postgresql
    database: activeagent_test
    username: postgres
    password: redrum
    host: localhost

  staging:
    adapter: postgresql
    database: activeagent_stage
    username: wiseleyb
    password: radiantDatabase
    host: 127.0.0.1

  production:
    adapter: postgresql
    database: activeagent
    username: wiseleyb
    password: radiantDatabase
    host: 192.168.1.139
  